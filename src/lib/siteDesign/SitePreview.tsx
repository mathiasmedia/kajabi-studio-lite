/**
 * Re-export of the SitePreview component from the engine package so font
 * + CSS-scope fixes auto-propagate to thin clients via
 * `bun update @k-studio-pro/engine`.
 */
export { SitePreview } from '@k-studio-pro/engine';
