/**
 * Re-export from the engine package so the blank-design baseline stays in
 * lockstep with the engine. Local edits are forbidden — change in master
 * and propagate via `bun update @k-studio-pro/engine`.
 */
export { buildBlankDesign } from "@k-studio-pro/engine";
